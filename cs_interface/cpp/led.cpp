#include <iostream.h>
#include "../../LED/led.h"
#include "led.hpp"
#include "packetizer.h" 

int main(int argc, char *argv[])
{



}

LED::send_all_color($`PIXEL color)
{
	uint8 led_buf[4];
	led_buf[0] = all;
	led_buf[1] = color.green;
	send(
}
